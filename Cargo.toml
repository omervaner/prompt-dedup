[package]
name = "prompt-dedup"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.31"
rusqlite = { version = "0.34", features = ["bundled"] }
rfd = "0.15"
image = { version = "0.25", default-features = false, features = ["png"] }

[build-dependencies]
winresource = "0.1"
